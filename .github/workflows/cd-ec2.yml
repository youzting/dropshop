name: CD-EC2-Docker

on:
  push:
    branches: ["main"]
  workflow_dispatch:

concurrency:
  group: cd-ec2-main
  cancel-in-progress: false

env:
  APP_NAME: dropshop
  DEPLOY_DIR: ${{ vars.EC2_DEPLOY_DIR }}
  MIN_DISK_MB: ${{ vars.EC2_MIN_DISK_MB || '1024' }}
  CD_ENABLED: ${{ vars.CD_ENABLED || 'false' }}

jobs:
  build-and-push:
    name: build-and-push-image
    if: ${{ vars.CD_ENABLED == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    outputs:
      full_image: ${{ steps.image-tag.outputs.full_image }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build Spring Boot jar
        run: ./gradlew --no-daemon --console=plain clean bootJar -x test

      - name: Resolve image repository
        id: image-repo
        run: echo "image_repo=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

      - name: Resolve image tag
        id: image-tag
        run: |
          echo "full_image=${{ steps.image-repo.outputs.image_repo }}:${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ steps.image-repo.outputs.image_repo }}:latest
            ${{ steps.image-tag.outputs.full_image }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-ec2:
    name: deploy-compose-to-ec2
    if: ${{ vars.CD_ENABLED == 'true' }}
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: production
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Add runner IP to EC2 security group
        id: allow-runner-ip
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
          SG_ID: ${{ secrets.EC2_SECURITY_GROUP_ID }}
        run: |
          RUNNER_IP=$(curl -sf https://checkip.amazonaws.com)
          echo "runner_ip=${RUNNER_IP}" >> "$GITHUB_OUTPUT"
          aws ec2 authorize-security-group-ingress \
            --group-id "${SG_ID}" \
            --protocol tcp \
            --port 22 \
            --cidr "${RUNNER_IP}/32"

      - name: Prepare production env file
        run: |
          printf '%s\n' "$PROD_ENV_FILE" > .env
          echo "APP_IMAGE=${{ needs.build-and-push.outputs.full_image }}" >> .env
        env:
          PROD_ENV_FILE: ${{ secrets.PROD_ENV_FILE }}

      - name: Copy compose files to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: ${{ secrets.EC2_PORT }}
          source: "docker-compose.prod.yml,.env,scripts/ec2-preflight.sh"
          target: "/tmp/${{ env.APP_NAME }}"

      - name: Cleanup sensitive files on runner
        if: always()
        run: rm -f .env

      - name: Deploy Docker Compose on EC2
        uses: appleboy/ssh-action@v1.0.3
        env:
          GHCR_USERNAME: ${{ secrets.GHCR_USERNAME }}
          GHCR_TOKEN: ${{ secrets.GHCR_PAT }}
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: ${{ secrets.EC2_PORT }}
          command_timeout: 20m
          envs: APP_NAME,DEPLOY_DIR,MIN_DISK_MB,GHCR_USERNAME,GHCR_TOKEN
          script: |
            set -euo pipefail
            trap 'echo "[deploy] failed at line $LINENO" >&2; rm -rf "/tmp/${APP_NAME}"' ERR
            set -x
            sudo mkdir -p "${DEPLOY_DIR}"
            sudo mv "/tmp/${APP_NAME}/docker-compose.prod.yml" "${DEPLOY_DIR}/docker-compose.yml"
            sudo mv "/tmp/${APP_NAME}/.env" "${DEPLOY_DIR}/.env"
            sudo mkdir -p "${DEPLOY_DIR}/scripts"
            sudo mv "/tmp/${APP_NAME}/scripts/ec2-preflight.sh" "${DEPLOY_DIR}/scripts/ec2-preflight.sh"
            sudo chown -R "${USER}:${USER}" "${DEPLOY_DIR}"
            cd "${DEPLOY_DIR}"

            chmod +x "${DEPLOY_DIR}/scripts/ec2-preflight.sh"
            "${DEPLOY_DIR}/scripts/ec2-preflight.sh" "${DEPLOY_DIR}" "${MIN_DISK_MB}"

            PREV_IMAGE="$(docker inspect --format='{{.Config.Image}}' dropshop-app 2>/dev/null || true)"
            if [ -n "${PREV_IMAGE}" ]; then
              echo "Detected previous image: ${PREV_IMAGE}"
            else
              echo "No existing dropshop-app container found."
            fi

            rollback() {
              echo "Attempting rollback..."
              if [ -n "${PREV_IMAGE}" ]; then
                if grep -q '^APP_IMAGE=' .env; then
                  sed -i "s|^APP_IMAGE=.*|APP_IMAGE=${PREV_IMAGE}|" .env
                else
                  echo "APP_IMAGE=${PREV_IMAGE}" >> .env
                fi
                docker compose --env-file .env -f docker-compose.yml up -d --remove-orphans || true
                echo "Rollback completed with previous image: ${PREV_IMAGE}"
              else
                echo "Rollback skipped: previous image not found."
              fi
            }

            dump_diagnostics() {
              echo "===== deploy diagnostics ====="
              docker compose --env-file .env -f docker-compose.yml ps || true
              docker inspect --format='container_state={{json .State}}' dropshop-app || true
              docker inspect --format='container_health={{json .State.Health}}' dropshop-app || true
              docker logs --tail=300 dropshop-app || true
              echo "===== end diagnostics ====="
            }

            set +x
            printf '%s\n' "${GHCR_TOKEN}" | docker login ghcr.io -u "${GHCR_USERNAME}" --password-stdin
            set -x

            set +e
            docker compose --env-file .env -f docker-compose.yml pull
            pull_status=$?

            up_status=0
            if [ "${pull_status}" -eq 0 ]; then
              docker compose --env-file .env -f docker-compose.yml up -d --remove-orphans
              up_status=$?
            fi
            set -e

            if [ "${pull_status}" -ne 0 ] || [ "${up_status}" -ne 0 ]; then
              echo "Deployment failed (pull=${pull_status}, up=${up_status})."
              dump_diagnostics
              rollback
              exit 1
            fi

            echo "Waiting for app to become healthy (up to 10m)..."
            health_status=0
            unhealthy_streak=0
            for i in $(seq 1 60); do
              STATUS="$(docker inspect --format='{{.State.Health.Status}}' dropshop-app 2>/dev/null || echo 'unknown')"
              echo "Attempt ${i}/60: health=${STATUS}"
              if [ "${STATUS}" = "healthy" ]; then
                echo "Health check passed (attempt ${i})"
                health_status=1
                unhealthy_streak=0
                break
              fi
              if [ "${STATUS}" = "unhealthy" ]; then
                unhealthy_streak=$((unhealthy_streak + 1))
                echo "Container unhealthy streak: ${unhealthy_streak}/3"
                if [ "${unhealthy_streak}" -ge 3 ]; then
                  echo "Container marked unhealthy 3 times, aborting."
                  break
                fi
              else
                unhealthy_streak=0
              fi
              sleep 10
            done

            if [ "${health_status}" -eq 0 ]; then
              echo "Health check failed after 600s."
              dump_diagnostics
              rollback
              exit 1
            fi

            docker compose --env-file .env -f docker-compose.yml ps

            rm -rf "/tmp/${APP_NAME}"

            docker image prune -af --filter "until=24h"

      - name: Remove runner IP from EC2 security group
        if: always()
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
          SG_ID: ${{ secrets.EC2_SECURITY_GROUP_ID }}
        run: |
          RUNNER_IP="${{ steps.allow-runner-ip.outputs.runner_ip }}"
          if [ -n "${RUNNER_IP}" ]; then
            aws ec2 revoke-security-group-ingress \
              --group-id "${SG_ID}" \
              --protocol tcp \
              --port 22 \
              --cidr "${RUNNER_IP}/32" || true
          fi
